import React from "react";
import { ScrollButton, ArrowUp } from "./ScrollUpElements";

const ScrollUp = () => {
  return (
    <ScrollButton>
      <ArrowUp />
    </ScrollButton>
  );
};

export default ScrollUp;
